# WebDev

-> Clean Code practices and usage of map, filter and reduce functions in javascript.
-> DOM Manipulation with JS, selecting nodes with querySelector(), functions related to nodes like appendChild(), insertBefore(), removeChild. 
-> Creating elements withh document.createElement('el'). then styling it with el.style.___; then appending this div to a parentNode.
-> Adding classes on the fly to an element in the document. Eg: div.classList.add("new"); also other functions like remove() and toggle()
-> Three general ways of event handling: in-HTML, on<eventtype> and eventListener.
-> Capturing phase, target phase and bubbling phase of event propagation. It's applications such as event delegation i.e. deciding at which point in heirarchy to act when an event occurs. Also methods like stopPropagation().
-> Creating new events with Event Constructor and Custom events with CustomEvent constructor. element.dispatchEvent(event) for dispatching the our custom event.
